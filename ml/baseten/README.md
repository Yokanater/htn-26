# ml/baseten (L2)

Placeholder. Baseten configuration and Python code live here (design §3.4, §6.2):

- BEI embedding deployment config (start it in Step 0, it is the long pole).
- Truss definitions, if any are needed.
- `distill/`: the distillation stack-up (improvement slot IS-DISTILL): labeling, fine-tuning on a
  Baseten H100 workstation, BEI classification deploy, and `distill/report.md` with the comparison.

Python only lives under `ml/`; everything else in the repo is TypeScript.

# Synthetic setup seed

Fictional merchants and product facts; never a live or market-demand claim. Seven owner sessions
save a collection; the seventh withdraws, leaving six eligible contributions in the illustrative
aggregate. `brief.json` is the first brief; `briefs.json` and matches/events link each distinct session.
These are contract fixtures; the S3 card must implement and verify the aggregate projection.
Image fixtures and actual provider recordings are supplied by human S0 spikes, not fabricated here.

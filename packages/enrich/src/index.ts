/**
 * @sei/enrich: public entry point. Owner: L2 (Intelligence).
 *
 * Baseten tagger + BEI embedder, the `enrich` / `resolve` / `score` stages, adjudicator and
 * component-judge prompts (run through the injected core `Reasoner`), `selectBundle`,
 * `createEnrichStages(env)`, fakes (RuleTagger, HashingEmbedder), CLIs.
 * Design §3.4, §5.6–§5.8, §6.2, §7, §8, §9.2. Allowed workspace imports: @sei/contracts, @sei/core.
 */
export {};
